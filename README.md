
  # Create It

  This is a code bundle for Create It. The original project is available at https://www.figma.com/design/M6IXuD5lceLQRoPfftrrZi/Create-It.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  